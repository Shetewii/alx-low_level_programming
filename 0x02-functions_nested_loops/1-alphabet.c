#include "main.h"
/**
* main - prints the alphabet, in lowercase
*
* Return : Always 0 (Success)
**/
int main (void){

int  letter;

for(letter = 'a'; letter <= 'z'; letter++)
{

	_putchar(letter);
}
       _ putchar('\n');

return(0);
}
