#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Prints a text according number
 *
 * Return: Always (Success)
 */
int main(void)
{
    int x;
    char latter ;

 for (x = 0; x <= 9 ;x++ )
 {
  printf("%d",x);
 }
 for(latter ='a';latter <= 'f';latter++)
 {
     printf("%c",latter);
 }






	return (0);

}

